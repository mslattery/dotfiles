- link:
    ~/.zshrc:
    ~/.bashrc:
    ~/.bash_profile:
    ~/.bash_prompt:

- shell:
    - command: apt update && apt install zsh -y
      stdout: true
      stderr: true
    - [chsh -s $(which zsh), Making zsh the default shell]
